services:
  dbservice:
    image: mysql:latest
    container_name: jobysDatabase
    ports:
      - "3305:3306"
    environment:
      MYSQL_ROOT_PASSWORD: jobys_pass
      MYSQL_DATABASE: jobys
    volumes:
      - "D:/Database/jobportal:/var/lib/mysql"